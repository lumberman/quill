{
  "schemaVersion": 1,
  "id": "quill.writer",
  "name": "Quill",
  "version": "1.0.0",
  "author": "Vlad Mitkovsky",
  "license": "MIT",
  "description": "Rewrite or spellcheck the selected text with a local model",
  "kinds": [
    "bar-widget"
  ],
  "entryPoints": {
    "barWidget": "BarWidget.qml"
  },
  "barWidget": {
    "displayName": "Quill",
    "description": "Local-AI writing assistant. Click to open the edit menu for the current selection; right-click for settings.",
    "category": "Status",
    "aliases": [
      "quill",
      "rewrite",
      "spellcheck",
      "writing"
    ],
    "allowMultiple": false,
    "defaultSection": "center",
    "defaults": {
      "command": "quill menu",
      "alwaysShow": true,
      "settingsCommand": "quill settings"
    },
    "schema": [
      {
        "key": "command",
        "type": "string",
        "label": "Command",
        "defaultValue": "quill menu",
        "description": "Run when the icon is clicked."
      },
      {
        "key": "settingsCommand",
        "type": "string",
        "label": "Settings command",
        "defaultValue": "quill settings",
        "description": "Run when the icon is right-clicked."
      },
      {
        "key": "alwaysShow",
        "type": "boolean",
        "label": "Always show",
        "defaultValue": true,
        "description": "When off, the icon only appears while Quill is working."
      }
    ]
  }
}
